# Django = web framework = set of tools to build interactive website
# Spec - We will build an app, Learning Log,
# allowing users to log the topics they're interested in and make journal entries as they learn about each topic. The
# Learning Log home page wil describe the site and invite users to either register or log in. Once logged,
# a user can create new topics, add new entries and read / edit existing entries

source ll_env/bin/activate = ll_env\Scripts\activate